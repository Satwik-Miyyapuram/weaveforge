"""Feature modules. Each is a vertical slice with the same internal shape
(``domain`` / ``application`` / ``infrastructure``) as the web app, so adding
one never requires editing another (see ``docs/DESIGN.md`` §3.1)."""
