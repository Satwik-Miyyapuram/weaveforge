import type { FeatureModule } from "@weaveforge/core";

/** Web app feature module — same as core FeatureModule; routes use static app page.tsx files. */
export type WebFeatureModule = FeatureModule;
