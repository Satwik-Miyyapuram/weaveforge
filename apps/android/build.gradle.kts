// The native inking shell (docs/internal/design/ink-native-bridges.md §2).
// Versions are pinned so a checkout builds the same APK next month.
plugins {
    id("com.android.application") version "8.7.3" apply false
    id("org.jetbrains.kotlin.android") version "2.0.21" apply false
}
